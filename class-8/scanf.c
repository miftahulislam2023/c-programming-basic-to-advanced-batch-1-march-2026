#include <stdio.h>

int main() {
    int x, y, z;
    char ch;
    scanf("%c", &ch);
    scanf("%d %d", &x, &y);
    scanf("%d", &z);
    printf("%d %d %d\n", x, y, z);
    printf("%c\n", ch);
    return 0;
}